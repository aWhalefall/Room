pluginManagement {
    repositories {
        maven { url 'https://oss.sonatype.org/content/repositories/snapshots' }
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url 'https://oss.sonatype.org/content/repositories/snapshots' }
        google()
        mavenCentral()
    }
}
rootProject.name = "JetPackRoom"
include ':app'
